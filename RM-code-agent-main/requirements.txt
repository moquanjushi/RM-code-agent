# DeepSeek ReAct 智能体 - Python 依赖包

# HTTP 请求库 - 用于调用 DeepSeek API
requests>=2.31.0,<3.0.0

# 环境变量管理 - 用于加载 .env 文件中的配置
python-dotenv>=1.0.0,<2.0.0

# 彩色终端输出（可选但推荐）- 用于美化 CLI 界面
colorama>=0.4.6,<1.0.0

# OpenAI 官方 SDK - 用于调用 OpenAI API
openai>=1.0.0

# Anthropic 官方 SDK - 用于调用 Claude API
anthropic>=0.18.0

# Google Generative AI SDK - 用于调用 Gemini API
google-genai

# Flask Web 框架 - 用于提供 Web API (v1.2.0 新增)
Flask>=3.0.0,<4.0.0

# Flask CORS - 用于跨域资源共享
flask-cors>=4.0.0,<5.0.0

# Flask-SocketIO - 用于 WebSocket 实时通信
flask-socketio>=5.3.0,<6.0.0

# Python-SocketIO - WebSocket 底层支持
python-socketio>=5.11.0,<6.0.0

# EventLet - 用于异步 WebSocket 支持 (可选)
eventlet>=0.35.0,<1.0.0
